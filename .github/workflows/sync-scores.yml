name: Sync JSONBin to Firebase

on:
  workflow_dispatch: 
    # Manual execution trigger only

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Run Python Sync Script
        run: |
          pip install requests
          
          python -c "
          import os
          import requests
          import json

          try:
              # 1. Secret Guardrails
              if not os.environ.get('FIREBASE_DB_SECRET'):
                  raise Exception('MISSING SECRET: FIREBASE_DB_SECRET')
              if not os.environ.get('JSONBIN_KEY'):
                  raise Exception('MISSING SECRET: JSONBIN_KEY')

              # 2. Fetch from JSONBin (Strict v3 Standards)
              print('📡 Fetching scores from JSONBin.io...')
              
              jsonbin_url = 'https://jsonbin.io'
              
              # X-Bin-Meta: false forces the server to return ONLY your raw data dictionary
              # This prevents 'record' wrapper errors and HTML responses
              jsonbin_headers = {
                  'X-Access-Key': os.environ.get('JSONBIN_KEY').strip(),
                  'X-Bin-Meta': 'false',
                  'Content-Type': 'application/json'
              }
              
              res = requests.get(jsonbin_url, headers=jsonbin_headers)
              
              # Debugging Block: Catch HTML/404 errors instantly
              if res.status_code != 200:
                  print(f'❌ JSONBin Server Error. Code: {res.status_code}')
                  print(f'❌ Raw Response (First 500 chars):\n{res.text[:500]}')
                  exit(1)
              
              try:
                  # With X-Bin-Meta: false, the response IS the payload. No ['record'] needed.
                  payload_data = res.json()
              except Exception as json_err:
                  print(f'❌ JSON Decode Failure! Raw text received:\n{res.text[:500]}')
                  raise json_err

              # 3. Push to Firebase (Singapore Endpoint + Legacy Auth)
              print('💾 Pushing data state to Firebase...')
              
              db_secret = os.environ.get('FIREBASE_DB_SECRET').strip()
              db_url = f'https://firebasedatabase.app{db_secret}'
              
              put_res = requests.put(db_url, json=payload_data)
              
              if put_res.status_code == 200:
                  print('✅ Sync completed successfully!')
              else:
                  print(f'❌ Firebase rejected write. Code: {put_res.status_code}. Response: {put_res.text}')
                  exit(1)

          except Exception as e:
              print(f'❌ CRITICAL FAILURE: {e}')
              exit(1)
          "
        env:
          FIREBASE_DB_SECRET: ${{ secrets.FIREBASE_DB_SECRET }}
          JSONBIN_KEY: ${{ secrets.JSONBIN_KEY }}
